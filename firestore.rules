rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TODO: Implement strict security rules for production
      allow read, write: if request.time < timestamp.date(2027, 12, 31);
    }
  }
}
